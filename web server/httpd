apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-apps
spec:
  replicas: 5
  selector:
   matchlabels:
     app: webserver
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - name: httpd-daemon
        image: httpd
